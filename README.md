OKAPI Authentication Example
============================

Installation
------------

1. Copy `config.php.dist` to `config.php` and add your OKAPI Consumer Keys
2. Run `composer install`
3. Open `authenticate.php` in a browser from your webserver
